<Page x:Class="Ressurection.Views.VersionPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      xmlns:prism="http://prismlibrary.com/"
      prism:ViewModelLocator.AutoWireViewModel="True"      
      d:DesignHeight="300" d:DesignWidth="300" Margin="5"
      Background="{DynamicResource MaterialDesignPaper}"
      FontFamily="{DynamicResource MaterialDesignFont}"
      Title="VersionPage">

    <Page.Resources>
        <Style TargetType="TextBlock" BasedOn="{StaticResource MaterialDesignSubheadingTextBlock}" x:Key="MenuTextBlock">
            <Setter Property="Padding" Value="10,0,10,0"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Page.Resources>
    <Grid >
        <StackPanel>
            <TextBlock x:Name="Setting" Text="Ressurection Version 0.0.1" Style="{DynamicResource MenuTextBlock}"/>
        </StackPanel>
    </Grid>
</Page>
